<%  if (breadcrumb.url != null && breadcrumb.url != "") {%>
    <span><a href="<%=breadcrumb.url%>"><%=breadcrumb.name%></a> </span>
    <%} else { %>
        <span><%=breadcrumb.name%></span>
        <%}%>
            <%  if (breadcrumb.breadcrumb != null) {%>
                /
                <%- include('./template_breadcrumb.ejs', {breadcrumb: breadcrumb.breadcrumb}); %>
                    <%}%>